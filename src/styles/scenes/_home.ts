import { StyleSheet } from "react-native";
import { _colors } from "../_global.styles";

export const _mainHeaderStyles = StyleSheet.create({
  container: {
    backgroundColor: _colors.primary,
  },
  logo: {
    marginLeft: 'auto',
    marginRight: 'auto',
  },
})